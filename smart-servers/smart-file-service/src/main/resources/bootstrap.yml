spring:
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.69.223:8848  # Nacos地址
        username: nacos
        password: nacos
        namespace: dev  # 命名空间
        group: file  # 分组
      config:
        server-addr: 192.168.69.223:8848  # Nacos地址
        username: nacos
        password: nacos
        namespace: dev  # 命名空间
        group: file  # 分组
        file-extension: yaml # 配置文件扩展名

